apiVersion: v1
kind: ConfigMap
metadata:
  name: ledgerflux-normalizer-config
  namespace: ledgerflux
data:
  config.json: |
    {
      "input_stream": "market_ticks",
      "output_stream": "market_normalized",
      "num_shards": 4,
      "shard_id": "auto"
    }

